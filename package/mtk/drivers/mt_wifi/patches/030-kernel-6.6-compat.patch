diff --git a/mt_wifi/chips/mt7981_dbg.c b/mt_wifi/chips/mt7981_dbg.c
index 0801fa4..8a663dd 100644
--- a/mt_wifi/chips/mt7981_dbg.c
+++ b/mt_wifi/chips/mt7981_dbg.c
@@ -4567,7 +4567,7 @@ static INT32 chip_show_coredump_proc(struct _RTMP_ADAPTER *pAd)
 	RTMP_STRING *msg;
 	UCHAR fileName[64];
 	struct file *file_w;
-	mm_segment_t orig_fs;
+	// mm_segment_t orig_fs;
 	UINT32 addr = 0;
 	UINT32 end_addr = 0;
 	UINT32 macVal = 0;
@@ -4580,8 +4580,8 @@ static INT32 chip_show_coredump_proc(struct _RTMP_ADAPTER *pAd)
 
 	NdisZeroMemory(msg, 4);
 
-	orig_fs = get_fs();
-	set_fs(KERNEL_DS);
+	// orig_fs = get_fs();
+	// set_fs(KERNEL_DS);
 
 	while (COREDUMP_QUEUE_INFO[i].Name != NULL) {
 		snprintf(fileName, sizeof(fileName), "/etc/%s.bin", COREDUMP_QUEUE_INFO[i].Name);
@@ -4626,7 +4626,7 @@ static INT32 chip_show_coredump_proc(struct _RTMP_ADAPTER *pAd)
 	}
 
 done:
-	set_fs(orig_fs);
+	// set_fs(orig_fs);
 	os_free_mem(msg);
 
 	return TRUE;
diff --git a/mt_wifi/chips/mt7986_dbg.c b/mt_wifi/chips/mt7986_dbg.c
index ee9ee89..831bfde 100644
--- a/mt_wifi/chips/mt7986_dbg.c
+++ b/mt_wifi/chips/mt7986_dbg.c
@@ -4831,7 +4831,7 @@ static INT32 chip_show_coredump_proc(struct _RTMP_ADAPTER *pAd)
 	RTMP_STRING *msg;
 	UCHAR fileName[64];
 	struct file *file_w;
-	mm_segment_t orig_fs;
+	//mm_segment_t orig_fs;
 	UINT32 addr = 0;
 	UINT32 end_addr = 0;
 	UINT32 macVal = 0;
@@ -4845,8 +4845,8 @@ static INT32 chip_show_coredump_proc(struct _RTMP_ADAPTER *pAd)
 
 	NdisZeroMemory(msg, 4);
 
-	orig_fs = get_fs();
-	set_fs(KERNEL_DS);
+	//orig_fs = get_fs();
+	//set_fs(KERNEL_DS);
 
 	while (COREDUMP_QUEUE_INFO[i].Name != NULL) {
 		ret = snprintf(fileName, sizeof(fileName), "/etc/%s.bin", COREDUMP_QUEUE_INFO[i].Name);
@@ -4895,7 +4895,7 @@ static INT32 chip_show_coredump_proc(struct _RTMP_ADAPTER *pAd)
 	}
 
 done:
-	set_fs(orig_fs);
+	//set_fs(orig_fs);
 	os_free_mem(msg);
 
 	return TRUE;
diff --git a/mt_wifi/embedded/ap/ap_cfg.c b/mt_wifi/embedded/ap/ap_cfg.c
index fdc66d8..ad995c6 100644
--- a/mt_wifi/embedded/ap/ap_cfg.c
+++ b/mt_wifi/embedded/ap/ap_cfg.c
@@ -17857,7 +17857,7 @@ INT Show_SimSectionUlm_Proc(RTMP_ADAPTER *pAd, RTMP_STRING *arg)
 {
 	UCHAR fileName[64];
 	struct file *file_w;
-	mm_segment_t orig_fs;
+	// mm_segment_t orig_fs;
 	UINT32 startaddr = 0,  endaddr = 0, addr = 0;
 	UINT32 macVal = 0;
 	UINT Idx;/* enable load from bin */
@@ -17898,8 +17898,8 @@ INT Show_SimSectionUlm_Proc(RTMP_ADAPTER *pAd, RTMP_STRING *arg)
 		endaddr = 0xE00D7FFF;
 	}
 
-	orig_fs = get_fs();
-	set_fs(KERNEL_DS);
+	// orig_fs = get_fs();
+	// set_fs(KERNEL_DS);
 	/* open file */
 	file_w = filp_open(fileName, O_WRONLY | O_CREAT, 0);
 
@@ -17939,7 +17939,7 @@ INT Show_SimSectionUlm_Proc(RTMP_ADAPTER *pAd, RTMP_STRING *arg)
 	}
 
 done:
-	set_fs(orig_fs);
+	// set_fs(orig_fs);
 
 	return TRUE;
 }
diff --git a/mt_wifi/embedded/ap/ap_mlme.c b/mt_wifi/embedded/ap/ap_mlme.c
index 16395fb..e09f1a2 100644
--- a/mt_wifi/embedded/ap/ap_mlme.c
+++ b/mt_wifi/embedded/ap/ap_mlme.c
@@ -26,7 +26,7 @@
  */
 
 #include "rt_config.h"
-#include <stdarg.h>
+#include <linux/stdarg.h>
 
 #ifdef IXIA_C50_MODE
 #ifdef MT7986
diff --git a/mt_wifi/embedded/common/hif_pci.c b/mt_wifi/embedded/common/hif_pci.c
index 384573d..abdbb1c 100644
--- a/mt_wifi/embedded/common/hif_pci.c
+++ b/mt_wifi/embedded/common/hif_pci.c
@@ -1855,7 +1855,7 @@ static INT rx_scatter_gather_copy(
 #if defined(CONFIG_WIFI_PAGE_ALLOC_SKB)
 			if (drop_scatter == FALSE) {
 				dma_sync_single_for_cpu((struct device *)(((POS_COOKIE)(pAd->OS_Cookie))->pDev),
-										(dma_addr_t)pCurRxD->SDP0, rx_ring->RxBufferSize, PCI_DMA_FROMDEVICE);
+										(dma_addr_t)pCurRxD->SDP0, rx_ring->RxBufferSize, DMA_FROM_DEVICE);
 				memcpy((cp_skb_pkt + buf_idx), cur_alloc_va, pCurRxD->SDL0);
 				buf_idx += pCurRxD->SDL0;
 			}
@@ -2059,7 +2059,7 @@ struct tx_rx_ctl *tr_ctl = &pAd->tr_ctl;
 		}
 
 		dma_sync_single_for_cpu((struct device *)(((POS_COOKIE)(pAd->OS_Cookie))->pDev),
-								(dma_addr_t)pRxD->SDP0, rx_ring->RxBufferSize, PCI_DMA_FROMDEVICE);
+								(dma_addr_t)pRxD->SDP0, rx_ring->RxBufferSize, DMA_FROM_DEVICE);
 
 		RTMP_DCACHE_FLUSH(cur_alloc_pa, pRxCell->DmaBuf.AllocSize);
 
diff --git a/mt_wifi/embedded/common/mlme.c b/mt_wifi/embedded/common/mlme.c
index 11213cc..c27e6af 100644
--- a/mt_wifi/embedded/common/mlme.c
+++ b/mt_wifi/embedded/common/mlme.c
@@ -25,7 +25,7 @@
 */
 
 #include "rt_config.h"
-#include <stdarg.h>
+#include <linux/stdarg.h>
 #ifdef DOT11R_FT_SUPPORT
 #include "ft.h"
 #endif /* DOT11R_FT_SUPPORT */
diff --git a/mt_wifi/embedded/common/rt_os_util.c b/mt_wifi/embedded/common/rt_os_util.c
index e88ae7c..01930c8 100644
--- a/mt_wifi/embedded/common/rt_os_util.c
+++ b/mt_wifi/embedded/common/rt_os_util.c
@@ -261,7 +261,7 @@ INT32  RtPrivIoctlSetVal(VOID)
 UINT32 MtRandom32(VOID)
 {
 #if (LINUX_VERSION_CODE  >= KERNEL_VERSION(3, 8, 0))
-	return prandom_u32();
+	return get_random_u32();
 #else /* LINUX_VERSION_CODE >= KERNEL_VERSION(3,8,0) */
 	return random32();
 #endif /* LINUX_VERSION_CODE < KERNEL_VERSION(3,8,0) */
diff --git a/mt_wifi/embedded/common/wdev.c b/mt_wifi/embedded/common/wdev.c
index 8eb8638..ad56413 100644
--- a/mt_wifi/embedded/common/wdev.c
+++ b/mt_wifi/embedded/common/wdev.c
@@ -623,8 +623,7 @@ INT32 wdev_attr_update(RTMP_ADAPTER *pAd, struct wifi_dev *wdev)
 				 __func__, wdev->wdev_idx, MAC2STR(wdev->if_addr)));
 
 		if (wdev->if_dev) {
-			NdisMoveMemory(RTMP_OS_NETDEV_GET_PHYADDR(wdev->if_dev),
-						   wdev->if_addr, MAC_ADDR_LEN);
+			dev_addr_set(wdev->if_dev,wdev->if_addr);
 		}
 
 		COPY_MAC_ADDR(wdev->bssid, wdev->if_addr);
@@ -637,7 +636,7 @@ INT32 wdev_attr_update(RTMP_ADAPTER *pAd, struct wifi_dev *wdev)
 				__func__, wdev->wdev_idx, MAC2STR(wdev->if_addr)));
 
 		if (wdev->if_dev)
-			NdisMoveMemory(RTMP_OS_NETDEV_GET_PHYADDR(wdev->if_dev), wdev->if_addr, MAC_ADDR_LEN);
+			dev_addr_set(wdev->if_dev, wdev->if_addr);
 		break;
 #endif
 	default:
diff --git a/mt_wifi/embedded/security/bn_lib.c b/mt_wifi/embedded/security/bn_lib.c
index 9c0b429..ff1957f 100644
--- a/mt_wifi/embedded/security/bn_lib.c
+++ b/mt_wifi/embedded/security/bn_lib.c
@@ -6399,7 +6399,7 @@ static int bnrand(int pseudorand, BIGNUM *rnd, int bits, int top, int bottom)
 {
 	unsigned char *buf = NULL;
 	int ret = 0, bit, bytes, mask;
-	time_t tim;
+	ktime_t tim;
 	UINT32 i;
 
 	if (bits == 0) {
diff --git a/mt_wifi/embedded/sta/sta.c b/mt_wifi/embedded/sta/sta.c
index 138bba3..7dd8ee2 100644
--- a/mt_wifi/embedded/sta/sta.c
+++ b/mt_wifi/embedded/sta/sta.c
@@ -583,8 +583,7 @@ INT sta_inf_open(struct wifi_dev *wdev)
 		if (!pAd->CommonCfg.bcfg80211Disabled) {
 			AsicSetWdevIfAddr(pAd, wdev, OPMODE_STA);
 			if (wdev->if_dev) {
-				NdisMoveMemory(RTMP_OS_NETDEV_GET_PHYADDR(wdev->if_dev),
-					wdev->if_addr, MAC_ADDR_LEN);
+				dev_addr_set(wdev->if_dev, wdev->if_addr);
 			}
 		}
 #endif /* IWCOMMAND_CFG80211_SUPPORT */
diff --git a/mt_wifi/include/os/rt_linux.h b/mt_wifi/include/os/rt_linux.h
index 761d848..d72dbdc 100644
--- a/mt_wifi/include/os/rt_linux.h
+++ b/mt_wifi/include/os/rt_linux.h
@@ -39,7 +39,6 @@
 #include <linux/ctype.h>
 #include <linux/vmalloc.h>
 
-
 #include <linux/wireless.h>
 #include <net/iw_handler.h>
 #if defined(CONFIG_TSO_SUPPORT) || defined(CONFIG_CSO_SUPPORT)
@@ -368,7 +367,7 @@ typedef struct file *RTMP_OS_FD;
 typedef struct _OS_FS_INFO_ {
 	int				fsuid;
 	int				fsgid;
-	mm_segment_t	fs;
+	// mm_segment_t	fs;
 } OS_FS_INFO;
 
 typedef struct _RTMP_OS_FD {
@@ -725,14 +724,14 @@ typedef struct os_cookie	*POS_COOKIE;
 #if (KERNEL_VERSION(4, 10, 0) > LINUX_VERSION_CODE)
 #define MTWF_PRINT	printk
 #else
-#define  printk(fmt, ...) printk(KERN_CONT fmt, ##__VA_ARGS__)
+#define  mt_printk(fmt, ...) printk(KERN_CONT fmt, ##__VA_ARGS__)
 #define MTWF_PRINT	pr_cont
 #endif
 #ifdef DBG_ENHANCE
 /* Mapping Wi-Fi driver log to syslog level */
 #define MTWF_PRINT_DBG_LVL_OFF		pr_crit
 #define MTWF_PRINT_DBG_LVL_ERROR	pr_err
-#define MTWF_PRINT_DBG_LVL_WARN		pr_warning
+#define MTWF_PRINT_DBG_LVL_WARN		pr_warn
 #define MTWF_PRINT_DBG_LVL_NOTICE	pr_notice
 #define MTWF_PRINT_DBG_LVL_INFO		pr_info
 #define MTWF_PRINT_DBG_LVL_DEBUG	pr_info
@@ -743,7 +742,7 @@ typedef struct os_cookie	*POS_COOKIE;
 #define ASSERT(x)                                                               \
 	{                                                                               \
 		if (!(x)) {                                                                   \
-			printk(KERN_WARNING __FILE__ ":%d assert " #x "failed\n", __LINE__);    \
+			mt_printk(KERN_WARNING __FILE__ ":%d assert " #x "failed\n", __LINE__);    \
 			dump_stack();\
 			/* panic("Unexpected error occurs!\n");					*/\
 		}                                                                           \
diff --git a/mt_wifi/include/os/rt_os.h b/mt_wifi/include/os/rt_os.h
index 14a68aa..4db4dda 100644
--- a/mt_wifi/include/os/rt_os.h
+++ b/mt_wifi/include/os/rt_os.h
@@ -33,7 +33,7 @@
 #define _RT_OS_H_
 
 /* SIOCDEVPRIVATE and SIOCIWFIRSTPRIV always exist in sockios.h and wireless.h until latest kernel v5.6*/
-#if (KERNEL_VERSION(5, 6, 0) > LINUX_VERSION_CODE)
+#if (KERNEL_VERSION(7, 6, 0) > LINUX_VERSION_CODE)
 
 #ifdef LINUX
 #if WIRELESS_EXT <= 11
diff --git a/mt_wifi/mcu/fwdl_mt.c b/mt_wifi/mcu/fwdl_mt.c
index 5928729..cc12b36 100644
--- a/mt_wifi/mcu/fwdl_mt.c
+++ b/mt_wifi/mcu/fwdl_mt.c
@@ -788,7 +788,7 @@ static NDIS_STATUS load_emi_fw(struct _RTMP_ADAPTER *pAd, enum target_cpu cpu, s
 			"emi_phy_addr = 0x%08x, emi_phy_addr_size = 0x%08x\n",
 			pChipCap->emi_phy_addr, pChipCap->emi_phy_addr_size);
 
-	vir_addr = ioremap_nocache(pChipCap->emi_phy_addr, pChipCap->emi_phy_addr_size);
+	vir_addr = ioremap(pChipCap->emi_phy_addr, pChipCap->emi_phy_addr_size);
 	if (vir_addr != 0) {
 		/* parsing and find download emi payload */
 		for (i = 0; i < target->num_of_region; i++) {
diff --git a/mt_wifi/os/linux/ap_ioctl.c b/mt_wifi/os/linux/ap_ioctl.c
index a5eaabe..bdb5ea1 100644
--- a/mt_wifi/os/linux/ap_ioctl.c
+++ b/mt_wifi/os/linux/ap_ioctl.c
@@ -579,8 +579,8 @@ INT32 rtmp_get_mgmtpwr(IN VOID *pAdSrc);
 INT rt28xx_ap_ioctl(void *net_dev_obj, void *data_obj, int cmd) /* snowpin for ap/sta */
 {
 	struct net_device *net_dev;
+	struct iwreq	*wrqin = (struct iwreq *) data_obj;
 	VOID			*pAd = NULL;
-    struct iwreq	*wrqin = (struct iwreq *) data_obj;
 	RTMP_IOCTL_INPUT_STRUCT rt_wrq, *wrq = &rt_wrq;
 	INT				Status = NDIS_STATUS_SUCCESS;
 	USHORT			subcmd;
@@ -1063,6 +1063,5 @@ LabelExit:
 		if (wrq->u.data.length != org_len)
 			wrqin->u.data.length = wrq->u.data.length;
 	}
-
 	return Status;
 }
diff --git a/mt_wifi/os/linux/rbus_prop_dev.c b/mt_wifi/os/linux/rbus_prop_dev.c
index b856b45..8e9a26b 100644
--- a/mt_wifi/os/linux/rbus_prop_dev.c
+++ b/mt_wifi/os/linux/rbus_prop_dev.c
@@ -89,12 +89,12 @@ static int DEVINIT mt_rbus_probe(struct pci_dev *pdev, const struct pci_device_i
 
 	MTWF_DBG(pAd, DBG_CAT_HIF, CATHIF_PCI, DBG_LVL_INFO, "===> rt2880_probe\n");
 
-	if (!pci_set_dma_mask(pdev, DMA_BIT_MASK(32))) {
+	if (!dma_set_mask(&pdev->dev, DMA_BIT_MASK(32))) {
 		/*
 		 * pci_set_consistent_dma_mask() will always be able to set the same
 		 * or a smaller mask as pci_set_dma_mask()
 		 */
-		pci_set_consistent_dma_mask(pdev, DMA_BIT_MASK(32));
+		dma_set_coherent_mask(&pdev->dev, DMA_BIT_MASK(32));
 	} else {
 		MTWF_DBG(pAd, DBG_CAT_HIF, CATHIF_PCI, DBG_LVL_ERROR,
 				 "set DMA mask failed!errno=%d\n", rv);
diff --git a/mt_wifi/os/linux/rt_linux.c b/mt_wifi/os/linux/rt_linux.c
index b748cb1..04bfb7e 100644
--- a/mt_wifi/os/linux/rt_linux.c
+++ b/mt_wifi/os/linux/rt_linux.c
@@ -1088,10 +1088,10 @@ static inline void __RtmpOSFSInfoChange(OS_FS_INFO *pOSFSInfo, BOOLEAN bSet)
 		/* pOSFSInfo->fsgid = (int)(current_fsgid()); */
 #endif
 #endif
-		pOSFSInfo->fs = get_fs();
-		set_fs(KERNEL_DS);
+		// pOSFSInfo->fs = get_fs();
+		// set_fs(KERNEL_DS);
 	} else {
-		set_fs(pOSFSInfo->fs);
+		// set_fs(pOSFSInfo->fs);
 #if (KERNEL_VERSION(2, 6, 29) > LINUX_VERSION_CODE)
 		current->fsuid = pOSFSInfo->fsuid;
 		current->fsgid = pOSFSInfo->fsgid;
@@ -1695,7 +1695,7 @@ int RtmpOSNetDevAddrSet(
 		}
 	}
 #endif /* CONFIG_STA_SUPPORT */
-	os_move_mem(net_dev->dev_addr, pMacAddr, 6);
+	dev_addr_set(net_dev, pMacAddr);
 	return 0;
 }
 
@@ -2029,8 +2029,7 @@ int RtmpOSNetDevAttach(
 
 #endif /* CONFIG_WIRELESS_EXT */
 		/* copy the net device mac address to the net_device structure. */
-		os_move_mem(pNetDev->dev_addr, &pDevOpHook->devAddr[0],
-					MAC_ADDR_LEN);
+		dev_addr_set(pNetDev, &pDevOpHook->devAddr[0]);
 		rtnl_locked = pDevOpHook->needProtcted;
 	}
 
@@ -2230,7 +2229,7 @@ VOID RtmpDrvAllMacPrint(
 {
 	struct file *file_w;
 	RTMP_STRING *fileName = "MacDump.txt";
-	mm_segment_t orig_fs;
+	// mm_segment_t orig_fs;
 	RTMP_STRING *msg;
 	UINT32 macAddr = 0, macValue = 0;
 	INT ret;
@@ -2240,8 +2239,8 @@ VOID RtmpDrvAllMacPrint(
 	if (!msg)
 		return;
 
-	orig_fs = get_fs();
-	set_fs(KERNEL_DS);
+	// orig_fs = get_fs();
+	// set_fs(KERNEL_DS);
 	/* open file */
 	file_w = filp_open(fileName, O_WRONLY | O_CREAT, 0);
 
@@ -2290,7 +2289,7 @@ VOID RtmpDrvAllMacPrint(
 		filp_close(file_w, NULL);
 	}
 
-	set_fs(orig_fs);
+	// set_fs(orig_fs);
 	os_free_mem(msg);
 }
 
@@ -2303,7 +2302,7 @@ VOID RtmpDrvAllE2PPrint(
 {
 	struct file *file_w;
 	RTMP_STRING *fileName = "EEPROMDump.txt";
-	mm_segment_t orig_fs;
+	// mm_segment_t orig_fs;
 	RTMP_STRING *msg;
 	USHORT eepAddr = 0;
 	USHORT eepValue;
@@ -2314,8 +2313,8 @@ VOID RtmpDrvAllE2PPrint(
 	if (!msg)
 		return;
 
-	orig_fs = get_fs();
-	set_fs(KERNEL_DS);
+	// orig_fs = get_fs();
+	// set_fs(KERNEL_DS);
 	/* open file */
 	file_w = filp_open(fileName, O_WRONLY | O_CREAT, 0);
 
@@ -2365,7 +2364,7 @@ VOID RtmpDrvAllE2PPrint(
 		filp_close(file_w, NULL);
 	}
 
-	set_fs(orig_fs);
+	// set_fs(orig_fs);
 	os_free_mem(msg);
 }
 
@@ -2377,10 +2376,10 @@ VOID RtmpDrvAllRFPrint(
 {
 	struct file *file_w;
 	RTMP_STRING *fileName = "RFDump.txt";
-	mm_segment_t orig_fs;
+	// mm_segment_t orig_fs;
 
-	orig_fs = get_fs();
-	set_fs(KERNEL_DS);
+	// orig_fs = get_fs();
+	// set_fs(KERNEL_DS);
 	/* open file */
 	file_w = filp_open(fileName, O_WRONLY | O_CREAT, 0);
 
@@ -2411,7 +2410,7 @@ VOID RtmpDrvAllRFPrint(
 		filp_close(file_w, NULL);
 	}
 
-	set_fs(orig_fs);
+	// set_fs(orig_fs);
 }
 
 
@@ -5715,8 +5714,7 @@ void os_system_tx_queue_dump(PNET_DEV dev)
 			if (skb->head) {
 				p = virt_to_head_page(skb->head);
 				page_size = PAGE_SIZE << compound_order(p);
-				printk("%s(): index:%d, page:%p ,rfcnt:%d, page size:%zu, order:%u, dtor:%u\n", __func__, k++, p,
-					OS_PAGE_REF(p), page_size, (unsigned int) compound_order(p), (unsigned int) p->compound_dtor);
+				printk("%s(): index:%d, page:%p ,rfcnt:%d, page size:%zu, order:%u\n", __func__, k++, p,OS_PAGE_REF(p), page_size, (unsigned int) compound_order(p));
 			}
 			skb = skb->next;
 		}
diff --git a/mt_wifi/os/linux/rt_main_dev.c b/mt_wifi/os/linux/rt_main_dev.c
index 6d6f18d..762c8ba 100644
--- a/mt_wifi/os/linux/rt_main_dev.c
+++ b/mt_wifi/os/linux/rt_main_dev.c
@@ -50,11 +50,8 @@ module_param(mode, charp, 0);
 MODULE_PARM_DESC(mac, "rt_wifi: wireless mac addr");
 MODULE_PARM_DESC(mode, "rt_wifi: wireless operation mode");
 
-#if !defined(CONFIG_PROPRIETARY_DRIVER) || defined(CONFIG_DBG_OOM)
 MODULE_LICENSE("GPL");
-#else
-MODULE_LICENSE("Proprietary");
-#endif
+
 
 #ifdef OS_ABL_SUPPORT
 RTMP_DRV_ABL_OPS RtmpDrvOps, *pRtmpDrvOps = &RtmpDrvOps;
@@ -85,7 +82,7 @@ static int rt_get_settings(struct net_device *dev, struct ethtool_cmd *cmd)
 	NdisZeroMemory(&req, sizeof(req));
 	rt28xx_ioctl(dev, (struct ifreq *)&req, SIOCGIWRATE);
 	cmd->speed = req.u.bitrate.value/1000000; /* The speed is Mbit/s */
-
+	printk("DBG2:: SIOCGIWRATE called, Rate = %lu",req.u.bitrate.value);
 	return 0;
 }
 
diff --git a/mt_wifi/os/linux/rt_rbus_pci_util.c b/mt_wifi/os/linux/rt_rbus_pci_util.c
index ee35e0b..15f4b45 100644
--- a/mt_wifi/os/linux/rt_rbus_pci_util.c
+++ b/mt_wifi/os/linux/rt_rbus_pci_util.c
@@ -160,7 +160,7 @@ PNDIS_PACKET RTMP_AllocateRxPacketBuffer(
 	}
 
 	if (pkt) {
-		*pa = dma_map_single(pdev, *va, size, PCI_DMA_FROMDEVICE);
+		*pa = dma_map_single(pdev, *va, size, DMA_FROM_DEVICE);
 		 if (dma_mapping_error(pdev, *pa)) {
 			MTWF_DBG(NULL, DBG_CAT_HIF, CATHIF_PCI, DBG_LVL_ERROR, "!!!!dma_mapping_error(pdev, *pa)!!!!\n");
 			switch (rx_ring->buf_type) {
@@ -232,10 +232,10 @@ ra_dma_addr_t linux_pci_map_single(void *pDev, void *ptr, size_t size, int sd_id
 	struct device *pdev = (struct device *)pDev;
 
 	if (direction == RTMP_PCI_DMA_TODEVICE)
-		direction = PCI_DMA_TODEVICE;
+		direction = DMA_TO_DEVICE;
 
 	if (direction == RTMP_PCI_DMA_FROMDEVICE)
-		direction = PCI_DMA_FROMDEVICE;
+		direction = DMA_FROM_DEVICE;
 
 	/*
 	 *	------ Porting Information ------
@@ -265,10 +265,10 @@ void linux_pci_unmap_single(void *pDev, ra_dma_addr_t radma_addr, size_t size, i
 	struct device *pdev = (struct device *)pDev;
 
 	if (direction == RTMP_PCI_DMA_TODEVICE)
-		direction = PCI_DMA_TODEVICE;
+		direction = DMA_TO_DEVICE;
 
 	if (direction == RTMP_PCI_DMA_FROMDEVICE)
-		direction = PCI_DMA_FROMDEVICE;
+		direction = DMA_FROM_DEVICE;
 
 	if (size > 0)
 		dma_unmap_single(pdev, DmaAddr, size, direction);
